# API Reference

The C++ source code is automatically documented via Doxygen & Make. 
you can view the generated documentation hosted via [github pages](https://andary22.github.io/LeftHookRoll/)
You can also build the reference locally:
```bash
make \
	docs # builds the documentation
	docs-clean # removes the generated documentation files
```